import { CollectionMenuComponent } from './collection-menu.component';

describe('CollectionMenuComponent', () => {
  let component: CollectionMenuComponent;

  beforeEach(() => {
    component = new CollectionMenuComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
