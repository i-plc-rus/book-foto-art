import { CollectionSortComponent } from './collection-sort.component';

describe('CollectionSortComponent', () => {
  let component: CollectionSortComponent;

  beforeEach(() => {
    component = new CollectionSortComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
