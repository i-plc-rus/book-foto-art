import { CollectionViewComponent } from './collection-view.component';

describe('CollectionViewComponent', () => {
  let component: CollectionViewComponent;

  beforeEach(() => {
    component = new CollectionViewComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
