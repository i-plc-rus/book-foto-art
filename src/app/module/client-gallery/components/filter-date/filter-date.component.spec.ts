import { FilterDateComponent } from './filter-date.component';

describe('FilterDateComponent', () => {
  let component: FilterDateComponent;

  beforeEach(() => {
    component = new FilterDateComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
