import { FilterDropdownComponent } from './filter-dropdown.component';

describe('FilterDropdownComponent', () => {
  let component: FilterDropdownComponent;

  beforeEach(() => {
    component = new FilterDropdownComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
