import { CollectionTableComponent } from './collection-table.component';

describe('CollectionTableComponent', () => {
  let component: CollectionTableComponent;

  beforeEach(() => {
    component = new CollectionTableComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
