import { CollectionSiteComponent } from './collection-site.component';

describe('CollectionSiteComponent', () => {
  let component: CollectionSiteComponent;

  beforeEach(() => {
    component = new CollectionSiteComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
