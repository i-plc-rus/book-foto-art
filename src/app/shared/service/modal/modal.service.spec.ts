import { ModalService } from './modal.service';

describe('ModalService', () => {
  const service: ModalService = new ModalService();

  it('should create', () => {
    expect(service).toBeTruthy();
  });
});
